import 'package:flutter/material.dart';

class AllSpendingPage extends StatelessWidget {
  const AllSpendingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Center(
          child: Text(
            'AllSpending',
          ),
        ),
      ],
    );
    ;
  }
}
